/**
 * Visual system: Astro Keel (https://kpab.github.io/astro-keel/).
 *
 * Light / dark is switched in the header (stored in localStorage).
 * Retune the whole palette by editing `--color-accent` in `src/styles/keel.css`.
 */
export const visualTheme = 'keel' as const;
